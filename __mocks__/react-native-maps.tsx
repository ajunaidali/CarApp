import React from 'react';
import { View } from 'react-native';

export default function MapView(props: React.ComponentProps<typeof View>) {
  return <View {...props} />;
}

export function Marker(props: React.ComponentProps<typeof View>) {
  return <View {...props} />;
}
